<%= form_with(model: meme, local: true) do |form| %>
  <% if meme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meme.errors.count, "error") %> prohibited this meme from being saved:</h2>

      <ul>
      <% meme.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, id: :meme_title, class: "form-control input-lg" %>
  </div>
  
  <div class="form-group">
    <%= form.label :image %>
    <%= form.file_field :image, id:"file-input", class: "form-control input-lg" %>
  </div>

  <div class="form-group">
    <%= form.submit 'Create Meme', class: "btn btn-success btn-lg" %>
  </div>
<% end %>
