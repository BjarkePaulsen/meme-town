<p id="notice"><%= notice %></p>

    <div class= "page-header" >  
        <h1>Memes <small>The Juiciest og Memes</small></h1>
    </div>
  <table>
    <thead>
      <tr>
        <th colspan="3"></th>
      </tr>
    </thead>
</div>

  <tbody>
    <% @memes.each do |meme| %>
      <tr>
        
 
    <% end %>
  </tbody>
  
  <center>
  <% @memes.each do |meme| %>
  <div class="jumbotron" style="max-width: 500px;">
    <h1>Hello, world!</h1>
    
    <p> 
        <%= image_tag meme.image(:large), class: "img-responsive" %> 
        <%= meme.title %> 
        <%= meme.user %> 
        <%= link_to 'Destroy', meme, method: :delete, data: { confirm: 'Are you sure?' } %> 
        <%= link_to 'Show', meme %>
        <%= link_to 'Edit', edit_meme_path(meme) %>
    </p>
    <p><a class="btn btn-primary btn-lg" href="#" role="button">Komments</a></p>
  </div>
  <% end %>
  </center>
  
  
</table>

<br>

<%= link_to 'New Meme', new_meme_path %>
